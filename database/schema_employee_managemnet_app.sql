  CREATE TABLE "BLOB_TEST" 
   (	"ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"IMAGE" BLOB, 
	"IMG_TYPE" VARCHAR2(500), 
	"FILE_NAME" VARCHAR2(100), 
	 CONSTRAINT "IMG_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   ) ;

  CREATE TABLE "APP_USER" 
   (	"USER_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"USER_NAME" VARCHAR2(30) NOT NULL ENABLE, 
	"USER_EMAIL" VARCHAR2(40) NOT NULL ENABLE, 
	"USER_PWD" VARCHAR2(20) NOT NULL ENABLE, 
	"ROLE" VARCHAR2(20) DEFAULT USER NOT NULL ENABLE, 
	 PRIMARY KEY ("USER_ID")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UNIQUE_EMAIL_ID" UNIQUE ("USER_EMAIL")
  USING INDEX  ENABLE
   ) ;

  CREATE TABLE "DEPARTMENTS" 
   (	"DEPT_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"DEPT_NAME" VARCHAR2(100), 
	"LOCATION" VARCHAR2(100), 
	"CREATED_AT" DATE DEFAULT SYSDATE, 
	 PRIMARY KEY ("DEPT_ID")
  USING INDEX  ENABLE
   ) ;

  CREATE TABLE "DESIGNATIONS" 
   (	"DESIGNATION_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"DESIGNATION_NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"MIN_SALARY" NUMBER(10,2) NOT NULL ENABLE, 
	"MAX_SALARY" NUMBER(10,2) NOT NULL ENABLE, 
	 CONSTRAINT "CHK_SALARY" CHECK (max_salary >=min_salary) ENABLE, 
	 PRIMARY KEY ("DESIGNATION_ID")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_DESIGNAME" UNIQUE ("DESIGNATION_NAME")
  USING INDEX  ENABLE
   ) ;

  CREATE TABLE "EMPLOYEES" 
   (	"EMP_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"FIRST_NAME" VARCHAR2(30) NOT NULL ENABLE, 
	"LAST_NAME" VARCHAR2(30) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(50) NOT NULL ENABLE, 
	"PHONE" VARCHAR2(10), 
	"HIRE_DATE" DATE DEFAULT SYSDATE NOT NULL ENABLE, 
	"DEPT_ID" NUMBER, 
	"DESIGNATION_ID" NUMBER, 
	"STATUS" VARCHAR2(20) DEFAULT 'ACTIVE' NOT NULL ENABLE, 
	"CREATED_BY" VARCHAR2(50) DEFAULT USER, 
	"CREATED_DATE" DATE DEFAULT SYSDATE, 
	"SALARY" NUMBER(10,2), 
	"MANAGER_ID" NUMBER, 
	 CONSTRAINT "CHK_EMP_STATUS" CHECK (status IN ('ACTIVE','INACTIVE','TERMINATED')) ENABLE, 
	 PRIMARY KEY ("EMP_ID")
  USING INDEX  ENABLE, 
	 CONSTRAINT "UQ_EMP_EMAIL" UNIQUE ("EMAIL")
  USING INDEX  ENABLE
   ) ;

  CREATE TABLE "PROJECT" 
   (	"PROJECT_ID" NUMBER GENERATED ALWAYS AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ROW_VERSION_NUMBER" NUMBER, 
	"PROJECT" VARCHAR2(100) NOT NULL ENABLE, 
	"TASK_NAME" VARCHAR2(200) NOT NULL ENABLE, 
	"START_DATE" DATE DEFAULT sysdate NOT NULL ENABLE, 
	"END_DATE" DATE, 
	"STATUS" VARCHAR2(20) DEFAULT ON NULL 'PENDING' NOT NULL ENABLE, 
	"EMP_ID" NUMBER, 
	"COST" NUMBER, 
	"BUDGET" NUMBER, 
	 PRIMARY KEY ("PROJECT_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "EMPLOYEES" ADD CONSTRAINT "FK_EMP_DEPT" FOREIGN KEY ("DEPT_ID")
	  REFERENCES "DEPARTMENTS" ("DEPT_ID") ENABLE;
  ALTER TABLE "EMPLOYEES" ADD CONSTRAINT "FK_EMP_DESIG" FOREIGN KEY ("DESIGNATION_ID")
	  REFERENCES "DESIGNATIONS" ("DESIGNATION_ID") ENABLE;

  ALTER TABLE "PROJECT" ADD CONSTRAINT "EMP_FORIGNKEY" FOREIGN KEY ("EMP_ID")
	  REFERENCES "EMPLOYEES" ("EMP_ID") ENABLE;
      
create or replace FUNCTION cust_validate_login (
    p_username IN VARCHAR2,
    p_password IN VARCHAR2
) RETURN BOOLEAN
AS
    l_user_id NUMBER;
BEGIN
    SELECT user_id INTO l_user_id
    FROM app_user
    WHERE UPPER(user_name) = UPPER(p_username)
      AND UPPER(user_pwd) = UPPER(p_password);

    APEX_UTIL.SET_SESSION_STATE(p_name => 'G_USER_ID', p_value => l_user_id);
    RETURN TRUE;
EXCEPTION
    WHEN no_data_found THEN
        RETURN FALSE;
END cust_validate_login;
/


